apiVersion: v1
kind: Service
metadata:
  name: spring-boot-todo-demo
spec:
  ports:
    - protocol: "TCP"
      port: 8080
      targetPort: 8080
  type: NodePort
  selector:
    app: spring-boot-todo-demo
